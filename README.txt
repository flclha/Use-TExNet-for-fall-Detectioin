预训练：预训练—动态权重
微调：微调_修复
预训练参数：IRM-pretrained-dynamic.pth

其余文件解释：
预训练-未加IRM：结果证明加IRM效果更好
预训练-固定两权重：固定CNN,Transformer初始权重都是0.5-动态权重对模型效果更好

fine_tune.pth  应该也是正确的预训练生成参数，但是找不到原来的预训练代码了。所以重跑了。这个应该也对。

微调_有滑窗： 可能因为有滑窗准确率虚高
微调-LOSO： 不加few-shot, 完全未知新用户数据分布，效果很差（召回率只有十几）———需要部分带标签数据适应新用户习惯
微调-初版无监督： 一个滑窗中大部分被判断为跌倒才是跌倒，未改过的微调模型（古早版本）
